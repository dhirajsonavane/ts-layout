import React from "react";
import Typography from "@material-ui/core/Typography";

const TrajectoryMaps: React.FC = () => {
  return (
    <div>
      <Typography variant="h3" component="h1">
      Trajectory Maps
      </Typography>
    </div>
  );
};

export default TrajectoryMaps;
